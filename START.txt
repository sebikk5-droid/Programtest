V27.6.35 – PWA / FILE SYSTEM TEST

Basis: BACKUP V27.6.32 ALLES OK.

Test:
- PWA manifest + service worker added.
- Existing app UI unchanged.
- History save remains single-report JSON.
- Chrome/Edge Save As unchanged.
- iOS Safari does NOT use navigator.share and does NOT auto-download in this
  diagnostic build. If no native save picker exists, a diagnostic message
  states whether the page is running as an installed PWA.
- This version is a capability test, not a promise that iPadOS exposes a
  folder picker to GitHub Pages.

For iPad test:
1. Open the page in Safari.
2. If desired, add to Home Screen and open the installed Web App.
3. Click Speichern.
4. Read the diagnostic result if Safari has no file picker.
